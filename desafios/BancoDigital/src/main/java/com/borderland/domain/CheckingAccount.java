package com.borderland.domain;

public class CheckingAccount extends BaseAccount {
    public CheckingAccount(int branch, int number, Customer customer) {
        super(branch, number, customer);
    }
}
